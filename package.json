{
  "name": "rhythmReader",
  "private": true,
  "version": "0.0.0",
  "description": "To help with transcribing rhythm",
    "devDependencies": {
    "express": "^4.13.3",
    "vexflow": "*"
  },

 
  "scripts": {
      "prestart": "npm install",
    "start": "http-server -a localhost -p 8000 -c-1"
  }
}